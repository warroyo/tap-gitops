apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: company-api-flux
spec:
  template:
    spec:
      volumes:
        - configMap:
            name: custom-vars
          name: custom-vars